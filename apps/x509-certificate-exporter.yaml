---
# discovered by discover-apps: referenced by 2 clusters
# renovate: datasource=docker depName=quay.io/enix/charts/x509-certificate-exporter
version: 4.2.0
helm:
  chartUrl: oci://quay.io/enix/charts/x509-certificate-exporter
