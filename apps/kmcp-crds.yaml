---
# discovered by discover-apps: referenced by 2 clusters
# renovate: datasource=docker depName=ghcr.io/kagent-dev/kmcp/helm/kmcp-crds
version: 0.3.0
helm:
  chartUrl: oci://ghcr.io/kagent-dev/kmcp/helm/kmcp-crds
