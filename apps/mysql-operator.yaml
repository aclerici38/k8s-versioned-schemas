---
# discovered by discover-apps: referenced by 2 clusters
# renovate: datasource=docker depName=ghcr.io/astrateam-net/oci-charts/mysql-operator
version: 2.3.0
helm:
  chartUrl: oci://ghcr.io/astrateam-net/oci-charts/mysql-operator
