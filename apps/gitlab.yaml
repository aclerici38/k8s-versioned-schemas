---
# discovered by discover-apps: referenced by 1 clusters
# renovate: datasource=docker depName=ghcr.io/linuzctl/helm-charts/gitlab
version: 10.2.2
helm:
  chartUrl: oci://ghcr.io/linuzctl/helm-charts/gitlab
