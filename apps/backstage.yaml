---
# discovered by discover-apps: referenced by 1 clusters
# renovate: datasource=docker depName=ghcr.io/backstage/charts/backstage
version: 2.10.0
helm:
  chartUrl: oci://ghcr.io/backstage/charts/backstage
