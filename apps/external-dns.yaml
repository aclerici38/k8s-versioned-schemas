---
# renovate: datasource=github-releases depName=kubernetes-sigs/external-dns
version: v0.20.0
# TODO: migrate to OCI chart once it exists
githubFolders:
  - https://github.com/kubernetes-sigs/external-dns/tree/${VERSION}/config/crd/standard
helm:
  valuesSchemaUrl: https://raw.githubusercontent.com/kubernetes-sigs/external-dns/refs/tags/${VERSION}/charts/external-dns/values.schema.json
