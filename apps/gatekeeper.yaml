---
# discovered by discover-apps: referenced by 1 clusters
# renovate: datasource=docker depName=ghcr.io/linuzctl/helm-charts/gatekeeper
version: 3.23.0
helm:
  chartUrl: oci://ghcr.io/linuzctl/helm-charts/gatekeeper
