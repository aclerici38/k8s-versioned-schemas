---
# discovered by discover-apps: referenced by 4 clusters
# renovate: datasource=docker depName=ghcr.io/kagent-dev/kagent/helm/kagent-crds
version: 0.10.0
helm:
  chartUrl: oci://ghcr.io/kagent-dev/kagent/helm/kagent-crds
