---
# discovered by discover-apps: referenced by 1 clusters
# renovate: datasource=docker depName=registry.k8s.io/lws/charts/lws
version: v0.10.0
helm:
  chartUrl: oci://registry.k8s.io/lws/charts/lws
