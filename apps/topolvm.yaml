---
# No OCI chart (charts are only on topolvm.github.io) and no values schema, so track the
# app image. The chart's templates/crds are helm-templated, so read the plain copy.
# renovate: datasource=docker depName=ghcr.io/topolvm/topolvm
version: 0.41.1
fileUrls:
  - https://raw.githubusercontent.com/topolvm/topolvm/refs/tags/v${VERSION}/config/crd/bases/topolvm.io_logicalvolumes.yaml
