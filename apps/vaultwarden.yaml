---
# discovered by discover-apps: referenced by 1 clusters
# renovate: datasource=docker depName=ghcr.io/astrateam-net/oci-charts/vaultwarden
version: 1.4.0
helm:
  chartUrl: oci://ghcr.io/astrateam-net/oci-charts/vaultwarden
