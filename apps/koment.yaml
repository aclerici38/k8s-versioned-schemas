---
# discovered by discover-apps: referenced by 1 clusters
# renovate: datasource=docker depName=ghcr.io/koment-dev/charts/koment
version: 3.1.0
helm:
  chartUrl: oci://ghcr.io/koment-dev/charts/koment
