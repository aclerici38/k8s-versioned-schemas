---
# discovered by discover-apps: referenced by 1 clusters
# renovate: datasource=docker depName=ghcr.io/hostzero-gmbh/charts/keycloak-operator
version: 0.11.2
helm:
  chartUrl: oci://ghcr.io/hostzero-gmbh/charts/keycloak-operator
