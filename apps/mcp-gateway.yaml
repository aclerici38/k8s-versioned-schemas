---
# discovered by discover-apps: referenced by 1 clusters
# renovate: datasource=docker depName=ghcr.io/kuadrant/charts/mcp-gateway
version: 0.9.0
helm:
  chartUrl: oci://ghcr.io/kuadrant/charts/mcp-gateway
