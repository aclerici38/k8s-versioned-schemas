---
# discovered by discover-apps: referenced by 3 clusters
# renovate: datasource=docker depName=cr.agentgateway.dev/charts/agentgateway-crds
version: v2.2.1
helm:
  chartUrl: oci://cr.agentgateway.dev/charts/agentgateway-crds
