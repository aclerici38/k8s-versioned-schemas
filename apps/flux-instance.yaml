---
# renovate: datasource=docker depName=ghcr.io/controlplaneio-fluxcd/charts/flux-operator
version: 0.44.0
helm:
  chartUrl: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-instance
  template: true
  requiredValues:
    instance:
      components:
        - source-controller
        - kustomize-controller
        - helm-controller
        - notification-controller
