---
# discovered by discover-apps: referenced by 3 clusters
# renovate: datasource=docker depName=ghcr.io/mariadb-operator/charts/mariadb-operator
version: 26.10.1
helm:
  chartUrl: oci://ghcr.io/mariadb-operator/charts/mariadb-operator
  requiredValues:
    crds:
      enabled: true
