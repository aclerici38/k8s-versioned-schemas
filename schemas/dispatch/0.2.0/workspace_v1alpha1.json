{
  "description": "Workspace is a project home on the NAS: git origin plus disposable cache.",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": [
        "string",
        "null"
      ]
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": [
        "string",
        "null"
      ]
    },
    "metadata": {
      "type": [
        "object",
        "null"
      ]
    },
    "spec": {
      "additionalProperties": false,
      "description": "WorkspaceSpec defines a project home: origin, storage, caches, lease.",
      "properties": {
        "caches": {
          "description": "Caches selects shared package-manager caches mounted into sessions\n(ADR-0006), e.g. node, uv, go.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "lease": {
          "additionalProperties": false,
          "description": "Lease serializes workspace access.",
          "properties": {
            "singleActiveSession": {
              "description": "SingleActiveSession allows at most one running session in this\nworkspace at a time (ADR-0005).",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": [
            "object",
            "null"
          ]
        },
        "origin": {
          "additionalProperties": false,
          "description": "Origin is the project's git remote.",
          "properties": {
            "defaultBranch": {
              "default": "main",
              "description": "DefaultBranch is the branch sessions base their work on.",
              "type": [
                "string",
                "null"
              ]
            },
            "gitUrl": {
              "description": "GitURL is the clone URL of the project remote.",
              "minLength": 1,
              "type": "string"
            }
          },
          "required": [
            "gitUrl"
          ],
          "type": "object"
        },
        "retention": {
          "additionalProperties": false,
          "description": "Retention bounds scratch lifetime.",
          "properties": {
            "scratch": {
              "description": "Scratch is how long session scratch directories are retained.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": [
            "object",
            "null"
          ]
        },
        "storage": {
          "additionalProperties": false,
          "description": "Storage shapes the backing PVC.",
          "properties": {
            "accessModes": {
              "description": "AccessModes of the claim; defaults to ReadWriteOnce, which the\nsingle-active-session lease is designed around. Set ReadWriteMany when\nthe storage class supports shared attach.",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "size": {
              "description": "Size of the workspace volume.",
              "oneOf": [
                {
                  "pattern": "^(\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))))?$",
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ],
              "x-kubernetes-int-or-string": true
            },
            "storageClassName": {
              "description": "StorageClassName selects the backing storage class.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "size"
          ],
          "type": "object"
        }
      },
      "required": [
        "origin",
        "storage"
      ],
      "type": [
        "object",
        "null"
      ]
    },
    "status": {
      "additionalProperties": false,
      "description": "WorkspaceStatus is the observed state of a Workspace.",
      "properties": {
        "activeSession": {
          "description": "ActiveSession holds the lease when SingleActiveSession is set.",
          "type": [
            "string",
            "null"
          ]
        },
        "conditions": {
          "description": "Conditions describe provisioning state.",
          "items": {
            "additionalProperties": false,
            "description": "Condition contains details for one aspect of the current state of this API Resource.",
            "properties": {
              "lastTransitionTime": {
                "description": "lastTransitionTime is the last time the condition transitioned from one status to another.\nThis should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.",
                "format": "date-time",
                "type": "string"
              },
              "message": {
                "description": "message is a human readable message indicating details about the transition.\nThis may be an empty string.",
                "maxLength": 32768,
                "type": "string"
              },
              "observedGeneration": {
                "description": "observedGeneration represents the .metadata.generation that the condition was set based upon.\nFor instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date\nwith respect to the current state of the instance.",
                "format": "int64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "reason": {
                "description": "reason contains a programmatic identifier indicating the reason for the condition's last transition.\nProducers of specific condition types may define expected values and meanings for this field,\nand whether the values are considered a guaranteed API.\nThe value should be a CamelCase string.\nThis field may not be empty.",
                "maxLength": 1024,
                "minLength": 1,
                "pattern": "^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$",
                "type": "string"
              },
              "status": {
                "description": "status of the condition, one of True, False, Unknown.",
                "enum": [
                  "True",
                  "False",
                  "Unknown"
                ],
                "type": "string"
              },
              "type": {
                "description": "type of condition in CamelCase or in foo.example.com/CamelCase.",
                "maxLength": 316,
                "pattern": "^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$",
                "type": "string"
              }
            },
            "required": [
              "lastTransitionTime",
              "message",
              "reason",
              "status",
              "type"
            ],
            "type": "object"
          },
          "type": [
            "array",
            "null"
          ],
          "x-kubernetes-list-map-keys": [
            "type"
          ],
          "x-kubernetes-list-type": "map"
        },
        "pvcName": {
          "description": "PVCName is the provisioned backing PersistentVolumeClaim.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": [
        "object",
        "null"
      ]
    }
  },
  "type": "object"
}