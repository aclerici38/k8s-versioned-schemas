{
  "description": "LaneProfile is a reusable model ensemble + framing referenced by CoderRuns.",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": [
        "string",
        "null"
      ]
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": [
        "string",
        "null"
      ]
    },
    "metadata": {
      "type": [
        "object",
        "null"
      ]
    },
    "spec": {
      "additionalProperties": false,
      "description": "LaneProfileSpec defines a reusable model ensemble and its runtime framing.\nIt is the seam that keeps Courier model-agnostic: roles may name cloud or\nlocal models in any mix, and nothing about a lane is baked into the core.",
      "properties": {
        "concurrency": {
          "default": 1,
          "description": "Concurrency is the maximum number of Running CoderRuns admitted on this\nlane. A local lane is typically 1; a cloud lane can be much higher.",
          "minimum": 1,
          "type": "integer"
        },
        "framing": {
          "description": "Framing is free-text lane context injected into the coordinator prompt:\nhardware reality, parallelism guidance, whether a load tool is available.",
          "type": [
            "string",
            "null"
          ]
        },
        "roles": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Roles maps a coordinator role (coordinator, coder, reviewer, ...) to a\nmodel identifier understood by the configured model gateway.",
          "type": "object"
        },
        "runtimeImage": {
          "description": "RuntimeImage selects the coordinator runtime and repository toolchain for\nruns on this lane. Empty uses the deployment's default coordinator image;\na lane may select any compatible image without changing Courier core.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "concurrency",
        "roles"
      ],
      "type": [
        "object",
        "null"
      ]
    }
  },
  "type": "object"
}