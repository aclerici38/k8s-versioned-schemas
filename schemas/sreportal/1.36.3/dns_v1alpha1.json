{
  "description": "DNS is the Schema for the dns API",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": [
        "string",
        "null"
      ]
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": [
        "string",
        "null"
      ]
    },
    "metadata": {
      "type": [
        "object",
        "null"
      ]
    },
    "spec": {
      "additionalProperties": false,
      "description": "spec defines the desired state of DNS",
      "properties": {
        "groups": {
          "description": "groups is a list of DNS entry groups for organizing entries in the UI",
          "items": {
            "additionalProperties": false,
            "description": "DNSGroup represents a group of DNS entries",
            "properties": {
              "description": {
                "description": "description is an optional description for the group",
                "type": [
                  "string",
                  "null"
                ]
              },
              "entries": {
                "description": "entries is a list of DNS entries in this group",
                "items": {
                  "additionalProperties": false,
                  "description": "DNSEntry represents a manual DNS entry",
                  "properties": {
                    "description": {
                      "description": "description is an optional description for the DNS entry",
                      "type": [
                        "string",
                        "null"
                      ]
                    },
                    "fqdn": {
                      "description": "fqdn is the fully qualified domain name",
                      "minLength": 1,
                      "type": "string"
                    }
                  },
                  "required": [
                    "fqdn"
                  ],
                  "type": "object"
                },
                "type": [
                  "array",
                  "null"
                ]
              },
              "name": {
                "description": "name is the display name for this group",
                "minLength": 1,
                "type": "string"
              }
            },
            "required": [
              "name"
            ],
            "type": "object"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "isRemote": {
          "description": "isRemote indicates this DNS resource is managed by the portal controller for a remote portal.\nWhen true, the DNS controller skips reconciliation and the portal controller manages the status directly.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "portalRef": {
          "description": "portalRef is the name of the Portal this DNS resource is linked to",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "portalRef"
      ],
      "type": "object"
    },
    "status": {
      "additionalProperties": false,
      "description": "status defines the observed state of DNS",
      "properties": {
        "conditions": {
          "description": "conditions represent the current state of the DNS resource.",
          "items": {
            "additionalProperties": false,
            "description": "Condition contains details for one aspect of the current state of this API Resource.",
            "properties": {
              "lastTransitionTime": {
                "description": "lastTransitionTime is the last time the condition transitioned from one status to another.\nThis should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.",
                "format": "date-time",
                "type": "string"
              },
              "message": {
                "description": "message is a human readable message indicating details about the transition.\nThis may be an empty string.",
                "maxLength": 32768,
                "type": "string"
              },
              "observedGeneration": {
                "description": "observedGeneration represents the .metadata.generation that the condition was set based upon.\nFor instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date\nwith respect to the current state of the instance.",
                "format": "int64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "reason": {
                "description": "reason contains a programmatic identifier indicating the reason for the condition's last transition.\nProducers of specific condition types may define expected values and meanings for this field,\nand whether the values are considered a guaranteed API.\nThe value should be a CamelCase string.\nThis field may not be empty.",
                "maxLength": 1024,
                "minLength": 1,
                "pattern": "^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$",
                "type": "string"
              },
              "status": {
                "description": "status of the condition, one of True, False, Unknown.",
                "enum": [
                  "True",
                  "False",
                  "Unknown"
                ],
                "type": "string"
              },
              "type": {
                "description": "type of condition in CamelCase or in foo.example.com/CamelCase.",
                "maxLength": 316,
                "pattern": "^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$",
                "type": "string"
              }
            },
            "required": [
              "lastTransitionTime",
              "message",
              "reason",
              "status",
              "type"
            ],
            "type": "object"
          },
          "type": [
            "array",
            "null"
          ],
          "x-kubernetes-list-map-keys": [
            "type"
          ],
          "x-kubernetes-list-type": "map"
        },
        "groups": {
          "description": "groups is the list of FQDN groups with their status",
          "items": {
            "additionalProperties": false,
            "description": "FQDNGroupStatus represents a group of FQDNs in the status",
            "properties": {
              "description": {
                "description": "description is the group description",
                "type": [
                  "string",
                  "null"
                ]
              },
              "fqdns": {
                "description": "fqdns is the list of FQDNs in this group",
                "items": {
                  "additionalProperties": false,
                  "description": "FQDNStatus represents the status of an aggregated FQDN",
                  "properties": {
                    "description": {
                      "description": "description is an optional description for the FQDN",
                      "type": [
                        "string",
                        "null"
                      ]
                    },
                    "fqdn": {
                      "description": "fqdn is the fully qualified domain name",
                      "type": "string"
                    },
                    "lastSeen": {
                      "description": "lastSeen is the timestamp when this FQDN was last observed",
                      "format": "date-time",
                      "type": "string"
                    },
                    "originRef": {
                      "additionalProperties": false,
                      "description": "originRef identifies the Kubernetes resource (Service, Ingress, DNSEndpoint)\nthat produced this FQDN via external-dns. Not set for manual entries.",
                      "properties": {
                        "kind": {
                          "description": "kind is the Kubernetes resource kind (e.g. Service, Ingress, DNSEndpoint)",
                          "type": "string"
                        },
                        "name": {
                          "description": "name is the name of the origin Kubernetes resource",
                          "type": "string"
                        },
                        "namespace": {
                          "description": "namespace is the Kubernetes namespace of the origin resource",
                          "type": "string"
                        }
                      },
                      "required": [
                        "kind",
                        "name",
                        "namespace"
                      ],
                      "type": [
                        "object",
                        "null"
                      ]
                    },
                    "recordType": {
                      "description": "recordType is the DNS record type (A, AAAA, CNAME, etc.)",
                      "type": [
                        "string",
                        "null"
                      ]
                    },
                    "syncStatus": {
                      "description": "syncStatus indicates whether the FQDN is correctly resolved in DNS.\nsync: the FQDN resolves to the expected type and targets.\nnotavailable: the FQDN does not exist in DNS.\nnotsync: the FQDN exists but resolves to different targets or type.",
                      "enum": [
                        "sync",
                        "notavailable",
                        "notsync",
                        ""
                      ],
                      "type": [
                        "string",
                        "null"
                      ]
                    },
                    "targets": {
                      "description": "targets is the list of target addresses for this FQDN",
                      "items": {
                        "type": "string"
                      },
                      "type": [
                        "array",
                        "null"
                      ]
                    }
                  },
                  "required": [
                    "fqdn",
                    "lastSeen"
                  ],
                  "type": "object"
                },
                "type": [
                  "array",
                  "null"
                ]
              },
              "name": {
                "description": "name is the group name",
                "type": "string"
              },
              "source": {
                "description": "source indicates where this group came from (manual, external-dns, or remote)",
                "enum": [
                  "manual",
                  "external-dns",
                  "remote"
                ],
                "type": "string"
              }
            },
            "required": [
              "name",
              "source"
            ],
            "type": "object"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "lastReconcileTime": {
          "description": "lastReconcileTime is the timestamp of the last reconciliation",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": [
        "object",
        "null"
      ]
    }
  },
  "required": [
    "spec"
  ],
  "type": "object"
}